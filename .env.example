# Production Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore for a reason!

# =============================================================================
# PUBLIC CLIENT-SIDE VARIABLES (Safe to use NEXT_PUBLIC_ prefix)
# =============================================================================
# These are baked into the JavaScript bundle and visible in the browser
# Only use NEXT_PUBLIC_ for values that are safe to expose publicly

# GitHub OAuth Configuration (Optional - for private repos)
# Get your client ID from: https://github.com/settings/developers
# Note: Client IDs are public by design, client secrets are NOT
# NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id

# EmailJS Configuration (Optional - for email notifications)
# These are public keys designed to be used client-side
# NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_emailjs_service_id
# NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
# NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_emailjs_public_key

# Application Configuration
NEXT_PUBLIC_APP_ENV=production

# =============================================================================
# SERVER-SIDE ONLY SECRETS (NEVER use NEXT_PUBLIC_ prefix!)
# =============================================================================
# These are only accessible in Node.js runtime and API routes
# NEVER expose these in the browser or they will be compromised

# Snyk API Token (Optional - for enhanced vulnerability scanning)
# Get your token from: https://app.snyk.io/account
# ⚠️ CRITICAL: Use SNYK_TOKEN, NOT NEXT_PUBLIC_SNYK_TOKEN
SNYK_TOKEN=your_snyk_token_here

# AWS Credentials (Optional - for AWS infrastructure scanning)
# Create IAM user with ECR, ECS, and Inspector2 read permissions
# ⚠️ CRITICAL: Use AWS_*, NOT NEXT_PUBLIC_AWS_*
# If you use NEXT_PUBLIC_ prefix, your AWS account will be compromised!
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# Google Gemini API Key (Optional - for AI-powered fix suggestions)
# Get your API key from: https://makersuite.google.com/app/apikey
# ⚠️ CRITICAL: Use GEMINI_API_KEY, NOT NEXT_PUBLIC_GEMINI_API_KEY
GEMINI_API_KEY=your_gemini_api_key

# =============================================================================
# SECURITY VERIFICATION
# =============================================================================
# After setting up, verify your credentials are not exposed:
# 1. Run: npm run build
# 2. Search build output: grep -r "your_actual_key" .next/static/
# 3. Expected result: No matches found
# 4. If you find matches, you used NEXT_PUBLIC_ prefix incorrectly!
